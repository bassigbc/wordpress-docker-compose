START WORDPRESS
run `docker-compose up -d` to start Wordpress on port 8000 ( http://localost:8000 or http://YOURIP:8000 )
WP file will be placed in ./site directory
To edit a preexistent WP site copy WP files in ./site directory EXCEPT wp-copnfig.php

--------

WP-CLI
To launch wp-cli commands use the following sintax:
docker run -it --rm --volumes-from wp-devel-site --network container:wp-devel-site wordpress:cli CLICOMMAND

--------

DB
#backup
docker exec mysql-db /usr/bin/mysqldump -uwordpress -pwordpress wordpress > db.sql

#restore
cat db.sql | docker exec mysql-db mysqldump -uwordpress -pwordpress wordpress

#command line client
docker exec -ti mysql-db mysql -uwordpress -pwordpress 


